# Optional public deployment origin used only for absolute Open Graph/Twitter image metadata.
# Must be an http(s) origin without credentials, path, query, or fragment.
# Leave unset for local-first deployments; no absolute metadata image URL is emitted.
WAYFINDER_PUBLIC_ORIGIN=https://wayfinder.example
